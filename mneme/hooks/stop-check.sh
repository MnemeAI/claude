#!/bin/bash
# Mneme Stop hook: advisory check for unsaved memory-worthy events.
# Always returns {"ok": true} — this is non-blocking.
# The real value is the statusMessage shown to the user.
echo '{"ok": true}'
